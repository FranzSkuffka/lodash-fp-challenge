














































































































































































































































































































// parse :: HttpResponse -> [[UserData]]
module.exports = _.flow([
  _.get('data'),
  log('data'),
  JSON.parse,
  _.map(_.mapKeys(_.camelCase)),
  _.chunk(3)
])
